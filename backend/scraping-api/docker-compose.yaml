services:
  mariadb:
    image: mariadb:latest
    container_name: mariadb-scraping
    environment:
      MARIADB_ROOT_PASSWORD: scraping
      MARIADB_DATABASE: ma_base_scraping
    volumes:
      - ./data:/var/lib/mysql
    ports:
      - "3306:3306"
    healthcheck:
      test: ["CMD", "healthcheck.sh", "--connect", "--innodb_initialized"]
      interval: 10s
      timeout: 5s
      retries: 5
